#include "3-calc.h"

/**
 * op_add - add two numbers
 * @a: the first number
 * @b: the second number
 * Return: a + b
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - substarct two numbers
 * @a: the first number
 * @b: te second number
 * Return: a - b
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multipling two numbers
 * @a: the first number
 * @b: the second number
 * Return: a * b
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - divided two numbers
 * @a: the first number
 * @b: the second number
 * Return: a / b
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - mod two numbers
 * @a: the first number
 * @b: the second number
 * Return: a % b
 */

int op_mod(int a, int b)
{
	return (a % b);
}
