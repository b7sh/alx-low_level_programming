#include <stdio.h>

/**
 * main - Entry point
 *
 * discripiton: print alphapets in lower case
 *
 * Return: Always 0 (success)
*/

#include <stdio.h>
int main(void)
{
char chr = 'a';

while (chr <= 'z')
{
putchar(chr);
chr++;
}
return (0);
}
