#include <stdio.h>

/**
 * main - Entry point
 *
 * discripiton: print the decimal number
 *
 * Return: Always 0 (success)
*/

int main(void)
{
int num;
for (num = 0; num < 10; num++)
{
printf("%d", num);

return (0);
}
