#include <stdio.h>

/**
 * main - Entry point
 *
 * discripiton: print the decimal number
 *
 * Return: Always 0 (success)
*/

int main(void)
{
int num = 0;
while (num <= 9)
{
printf("%d", num);
num++;
}
printf('\n');
return (0);
}
