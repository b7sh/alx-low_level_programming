#include <stdio.h>

/**
 * main - Entry point
 *
 * discripiton: print the hexadecimal in lower case
 *
 * Return: Always 0 (success)
*/

int main(void)
{
int n;

for (n = 0; n <= 15; n++)
{
putchar(n);
}
putchar('\n');

return (0);
}

