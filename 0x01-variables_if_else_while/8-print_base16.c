#include <stdio.h>

/**
 * main - Entry point
 *
 * discripiton: print the hexadecimal in lower case
 *
 * Return: Always 0 (success)
*/

int main(void)
{
int number = 48;
while (number <= 102)
{
putchar(number);
if (number == 57)
digit += 39;
digit++;
}
putchar('\n');

return (0);
}

