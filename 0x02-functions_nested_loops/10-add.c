#include "main.h"

/**
 * add - function that add tow integers and return to the result
* @bro: the first integer will add to sis
* @sis: the second integer will add to bro
* Return: add tow numbrs
*/

int add(int bro, int sis)
{
int me = bro + sis;
return (me);
}
